language: python

python:
  - "2.7"

services:
  - redis-server

install:
  - "pip install --user -r requirements.txt"
  - "pip install --user -r dev-requirements.txt"

script:
  - "py.test"

after_success:
  - "coveralls"

notifications:
  email: false

sudo: false
